# SMTP Configuration (app.py: SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS, MAIL_TO)
# Copy to .env and fill in your values

# Gmail: use App Password (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=windowsrayyan@gmail.com
SMTP_PASS=opuo tqau texk ybxm

# Rayyan receives enquiries here. Reply-To = client email (so Rayyan can reply to client).
# Client also gets auto "Thank you" mail.
MAIL_TO=windowsrayyan@gmail.com
